import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Banner = () => {
  return <View>Start Working With Banner</View>;
};

const styles = StyleSheet.create({
  container: {},
});

export default Banner;
